# .env.local
# Use this file only for private secrets that should never be checked into source control.
# The Standard Edition works without AI or external key configuration.
VITE_GROQ_API_KEY=

# .env.example
# Copy this file to .env.local if you later use internal premium workflow keys.
VITE_GROQ_API_KEY=
